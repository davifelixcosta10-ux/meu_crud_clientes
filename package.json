{
  "name": "davi-flow-wt-medio",
  "version": "1.0.0",
  "description": "Painel em `daviflow.vercel.app` + `daviflowgestoes.vercel.app` — FastAPI + Supabase + Vanilla JS + Tailwind.",
  "main": "app.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
